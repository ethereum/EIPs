on:
  pull_request_target:

jobs:
  jekyll-label-action:
    name: Automatic Label Bot
    runs-on: ubuntu-latest
    
    steps:
      - uses: Pandapip1/jekyll-label-action@1fc28bce56ed5b2fe79161718b630b620128a561
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
