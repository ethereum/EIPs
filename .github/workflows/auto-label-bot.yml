on:
  pull_request_target:

jobs:
  jekyll-label-action:
    name: Automatic Label Bot
    runs-on: ubuntu-latest
    
    steps:
      - uses: Pandapip1/jekyll-label-action@71232f98d39d59c2bf1001c006686ea801eaf929
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
