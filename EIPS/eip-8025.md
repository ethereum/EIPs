---
eip: 8025
title: Optional Execution Proofs
description: Introducing optional execution proofs on the consensus layer
author: Kevaundray Wedderburn (@kevaundray), Justin Drake (@JustinDrake) <justin@ethereum.org>, Ignacio Hagopian (@jsign), Han (@han0110), Francesco Risitano (@frisitano)
discussions-to: https://ethereum-magicians.org/t/eip-optional-execution-proofs/25500
status: Draft
type: Standards Track
category: Core
created: 2025-09-17
---

## Abstract

Execution proofs enable consensus layer (CL) nodes to perform stateless validation of execution payloads. Altruistic *proof-generating nodes* produce execution proofs and broadcast them over the CL p2p network. *Proof-verifying nodes* consume gossiped execution proofs to assert the validity of execution payloads. Verification is *stateless* and sublinear in the gas limit, so a validator's resource requirements scale sublinearly, an improvement on the linear cost of re-execution. The mechanism is fully opt-in and changes no consensus rules, allowing the proving stack to mature in production before future promotion in mandatory proofs.

## Motivation

Today, verifying a beacon block requires re-execution of its execution payload over the EL state. The cost of this re-execution scales linearly with the gas limit, and it requires the node to maintain the entire EL state. This couples a node's resource requirements to the state size and the chain's throughput.

This EIP introduces an opt-in path with the following properties:

- **Sublinear verification cost.** Verifying an execution proof takes approximately constant work, regardless of how much gas the underlying execution payload consumes.
- **Stateless verification.** Verifying an execution proof requires only the proof itself and a public commitment to the payload — not the EL state.
- **Enhanced decentralization.** Sublinear, stateless verification keeps node operation tractable for participants with modest hardware and expertise, even as the gas limit and state grow.
- **Optional by design.** An optional first step lets clients build operational experience, collect performance metrics, and fine-tune the execution proof stack based on real-world findings — without network-wide consensus implications.

## Specification

The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in [RFC 2119](https://www.rfc-editor.org/rfc/rfc2119) and [RFC 8174](https://www.rfc-editor.org/rfc/rfc8174).

This EIP defines two opt-in roles and the artefacts they exchange. Detailed specifications are maintained in the consensus-specs and execution-specs repositories seen below:

- [Consensus Layer](https://github.com/ethereum/consensus-specs/tree/5aa6eec83af11bb67e7ff69aa647b5736f530342/specs/_features/eip8025)
- Execution Layer:
    - [Stateless validation interfaces](https://github.com/ethereum/execution-specs/blob/ec23140720d6a9257a907c470ba1874623bd7b50/src/ethereum/forks/amsterdam/stateless.py)
    - [Guest program](https://github.com/ethereum/execution-specs/blob/ec23140720d6a9257a907c470ba1874623bd7b50/src/ethereum/forks/amsterdam/stateless_guest.py)
    - [Host-side input assembly](https://github.com/ethereum/execution-specs/blob/ec23140720d6a9257a907c470ba1874623bd7b50/src/ethereum/forks/amsterdam/stateless_host.py)
    - [Execution witness construction](https://github.com/ethereum/execution-specs/blob/ec23140720d6a9257a907c470ba1874623bd7b50/src/ethereum/forks/amsterdam/stateless_host_exec_witness.py)
    - [SSZ serialization schema](https://github.com/ethereum/execution-specs/blob/ec23140720d6a9257a907c470ba1874623bd7b50/src/ethereum/forks/amsterdam/stateless_ssz.py)
    - [Conformance tests](https://github.com/ethereum/execution-specs/tree/ec23140720d6a9257a907c470ba1874623bd7b50/tests/amsterdam/eip8025_optional_proofs)

### Consensus Layer

A node MAY enable either or both of the following modes:

- **Proof-generating mode.** On receiving a valid block, the node requests an execution proof from the proof engine and broadcasts it over a dedicated gossipsub topic.
- **Proof-verifying mode.** Upon receiving an execution proof, the node provides it to the proof engine for verification. If the proof engine returns a `VALID` result, the fork choice is updated. Proof verification does not require EL state.

At initial deployment, opted-in nodes are strongly recommended to continue running an execution node alongside; local re-execution remains the primary source of fork-choice validity and proofs serve as a supplementary check.

### Execution Layer

This EIP introduces two main execution-layer components:

- The **stateless input**: the data consumed by the proving guest (i.e. program being proven). It contains the execution payload request, the witness material needed to execute the block, and chain configuration.
- The **guest program**: the statement attested to by an execution proof. It deserializes the stateless input, validates it, executes the payload through the same `new_payload` path used by the execution engine, and returns a compact validation result.

At a high level, proof generation follows this flow:

1. A host, usually a stateful execution-layer client, re-executes/constructs the block and records the account, storage, code, and ancestor-header data used during execution. 
2. The host builds an `ExecutionWitness`, wraps it with a `NewPayloadRequest`, chain configuration, and recovered transaction public keys into a `StatelessInput`, and serializes that input as schema-prefixed SSZ bytes.
3. A prover runs the guest program on the serialized `StatelessInput`. The guest program validates the chain configuration and witness scaffolding, re-executes the payload against witness-backed state, and returns `StatelessValidationResult`.

The top-level input consumed by the guest is `StatelessInput`:

```python
class StatelessInput:
    new_payload_request: NewPayloadRequest
    witness: ExecutionWitness
    chain_config: ChainConfig
    public_keys: Tuple[Bytes, ...]
```

At this level:

- `new_payload_request` is the Engine API payload data supplied by the consensus layer.
- `witness` is the account, storage, code, and ancestor-header data needed to execute the payload without access to local EL state.
- `chain_config` tells the guest which chain ID and fork configuration to validate against.
- `public_keys` contains recovered transaction public keys, avoiding full signature recovery inside the guest. Txs signatures must still be validated.

The `new_payload_request` field represents `newPayload` data, including typed execution requests:

```python
class NewPayloadRequest:
    execution_payload: ExecutionPayload
    versioned_hashes: Tuple[VersionedHash, ...]
    parent_beacon_block_root: Root
    execution_requests: ExecutionRequests


class ExecutionPayload:
    # Identical to the consensus-specs ExecutionPayload container.
    ...


class ExecutionRequests:
    # Identical to the consensus-specs ExecutionRequests container.
    ...
```

The remaining top-level fields define the witness and execution context used by the guest:

```python
class ExecutionWitness:
    state: Tuple[Bytes, ...]
    codes: Tuple[Bytes, ...]
    headers: Tuple[Bytes, ...]


class ChainConfig:
    chain_id: U64
    active_fork: ForkConfig


class ForkConfig:
    fork: ProtocolFork
    activation: ForkActivation
    blob_schedule: BlobSchedule | None


class ForkActivation:
    block_number: U64 | None
    timestamp: U64 | None


class BlobSchedule:
    target: U64
    max: U64
    base_fee_update_fraction: U64


class ProtocolFork(StrEnum):
    ...
    Amsterdam = "Amsterdam"
```

The guest returns a compact validation result:

```python
class StatelessValidationResult:
    new_payload_request_root: Hash32
    successful_validation: bool
    chain_config: ChainConfig
```

The `ExecutionWitness` fields have the following roles:

- `state`: RLP-encoded trie-nodes needed during execution and state-root recomputation.
- `codes`: bytecodes required for code reads from the pre-state. Code created in the block being executed is not included because the guest observes it during re-execution.
- `headers`: RLP-encoded parent and ancestor headers, ordered by block number, used resolve `BLOCKHASH` opcodes. The guest checks that these headers form a contiguous chain.

The SSZ schema mirrors the dataclasses and applies bounded-list limits to the wire representation:

```python
# Current maximum sizes are still not final and further tuned.
MAX_WITNESS_NODES = 2**20
MAX_WITNESS_CODES = 2**16
MAX_WITNESS_HEADERS = 256
MAX_BYTES_PER_WITNESS_NODE = 2**20
MAX_BYTES_PER_CODE = 2**24
MAX_BYTES_PER_HEADER = 2**10
MAX_DEPOSIT_REQUESTS_PER_PAYLOAD = 2**13
MAX_WITHDRAWAL_REQUESTS_PER_PAYLOAD = 2**4
MAX_CONSOLIDATION_REQUESTS_PER_PAYLOAD = 2**1
MAX_BLOB_COMMITMENTS_PER_BLOCK = 4096
MAX_OPTIONAL_FORK_ACTIVATION_VALUES = 1
MAX_BLOB_SCHEDULES_PER_FORK = 1
MAX_PUBLIC_KEYS = 2**20
PUBLIC_KEY_BYTES = 65
STATELESS_INPUT_SCHEMA_ID = 0x0001
STATELESS_INPUT_SCHEMA_ID_SIZE = 2
STATELESS_INPUT_SCHEMA_ID_BYTES = STATELESS_INPUT_SCHEMA_ID.to_bytes(
    STATELESS_INPUT_SCHEMA_ID_SIZE,
    "big",
)


class SszExecutionWitness(Container):
    state: SszList[ByteList[MAX_BYTES_PER_WITNESS_NODE], MAX_WITNESS_NODES]
    codes: SszList[ByteList[MAX_BYTES_PER_CODE], MAX_WITNESS_CODES]
    headers: SszList[ByteList[MAX_BYTES_PER_HEADER], MAX_WITNESS_HEADERS]


class SszDepositRequest(Container):
    # Identical to the consensus-specs DepositRequest container.
    ...


class SszWithdrawalRequest(Container):
    # Identical to the consensus-specs WithdrawalRequest container.
    ...


class SszConsolidationRequest(Container):
    # Identical to the consensus-specs ConsolidationRequest container.
    ...


class SszExecutionRequests(Container):
    deposits: SszList[SszDepositRequest, MAX_DEPOSIT_REQUESTS_PER_PAYLOAD]
    withdrawals: SszList[
        SszWithdrawalRequest, MAX_WITHDRAWAL_REQUESTS_PER_PAYLOAD
    ]
    consolidations: SszList[
        SszConsolidationRequest, MAX_CONSOLIDATION_REQUESTS_PER_PAYLOAD
    ]


class SszExecutionPayload(Container):
    ...


class SszNewPayloadRequest(Container):
    execution_payload: SszExecutionPayload
    versioned_hashes: SszList[Bytes32, MAX_BLOB_COMMITMENTS_PER_BLOCK]
    parent_beacon_block_root: Bytes32
    execution_requests: SszExecutionRequests


class SszForkActivation(Container):
    block_number: SszList[uint64, MAX_OPTIONAL_FORK_ACTIVATION_VALUES]
    timestamp: SszList[uint64, MAX_OPTIONAL_FORK_ACTIVATION_VALUES]


class SszBlobSchedule(Container):
    target: uint64
    max: uint64
    base_fee_update_fraction: uint64


class SszForkConfig(Container):
    fork: uint64
    activation: SszForkActivation
    blob_schedule: SszList[SszBlobSchedule, MAX_BLOB_SCHEDULES_PER_FORK]


class SszChainConfig(Container):
    chain_id: uint64
    active_fork: SszForkConfig


class SszStatelessInput(Container):
    new_payload_request: SszNewPayloadRequest
    witness: SszExecutionWitness
    chain_config: SszChainConfig
    public_keys: SszList[ByteVector[PUBLIC_KEY_BYTES], MAX_PUBLIC_KEYS]


class SszStatelessValidationResult(Container):
    new_payload_request_root: Bytes32
    successful_validation: boolean
    chain_config: SszChainConfig
```

The host serializes a `StatelessInput` by prefixing the SSZ bytes with `STATELESS_INPUT_SCHEMA_ID`. Given that guest programs must support multiple forks, this allows the guest program to know which is the target `StatelessInput` shape given that it might change at fork boundaries, or potential new encoding schemes. 

```python
def serialize_stateless_input(
    stateless_input: StatelessInput,
) -> Bytes:
    ssz_obj = stateless_input_to_ssz(stateless_input)
    return Bytes(
        STATELESS_INPUT_SCHEMA_ID_BYTES + bytes(ssz_obj.encode_bytes())
    )
```

The guest entry point is intentionally small: it decodes the schema-prefixed SSZ input, runs stateless payload validation, and serializes the result.

```python
def deserialize_stateless_input(data: Bytes) -> StatelessInput:
    if len(data) < STATELESS_INPUT_SCHEMA_ID_SIZE:
        raise ValueError("Stateless input is missing schema id")
    schema_id = int.from_bytes(
        data[:STATELESS_INPUT_SCHEMA_ID_SIZE],
        "big",
    )
    if schema_id != STATELESS_INPUT_SCHEMA_ID:
        raise ValueError(
            f"Unsupported stateless input schema id: 0x{schema_id:04x}"
        )
    ssz_obj = SszStatelessInput.decode_bytes(
        data[STATELESS_INPUT_SCHEMA_ID_SIZE:]
    )
    return ssz_to_stateless_input(ssz_obj)


def run_stateless_guest(input_bytes: Bytes) -> Bytes:
    stateless_input = deserialize_stateless_input(input_bytes)
    stateless_output = verify_stateless_new_payload(stateless_input)

    output_bytes = serialize_stateless_output(stateless_output)
    return output_bytes
```

The core guest validation computes a public commitment to the payload request (used by the proof verifier to have a short commitment to which payload is was validated), validates that the configured active fork can execute the payload, creates a `WitnessState`, and then delegates block validation and execution to the normal new-payload execution path:

```python
def validate_chain_config(
    chain_config: ChainConfig,
    new_payload_request: NewPayloadRequest,
) -> ForkConfig:
    active_fork = chain_config.active_fork
    execution_payload = new_payload_request.execution_payload

    if not _is_activation_active(active_fork.activation, execution_payload):
        raise InactiveForkConfigError(
            "ChainConfig active_fork is not active for the target payload"
        )

    if active_fork.fork != ProtocolFork.Amsterdam:
        raise UnsupportedForkConfigError(
            f"Amsterdam stateless guest cannot execute {active_fork.fork}"
        )

    if active_fork.blob_schedule != _expected_amsterdam_blob_schedule():
        raise UnsupportedForkConfigError(
            "ChainConfig active_fork blob_schedule does not match Amsterdam"
        )

    return active_fork


def verify_stateless_new_payload(
    stateless_input: StatelessInput,
) -> StatelessValidationResult:
    new_payload_request_root = compute_new_payload_request_root(
        stateless_input
    )
    witness = stateless_input.witness

    try:
        validate_chain_config(
            stateless_input.chain_config,
            stateless_input.new_payload_request,
        )

        decoded_headers, block_hashes = validate_headers(witness.headers)
        parent_header = decoded_headers[-1]

        chain_context = ChainContext(
            chain_id=stateless_input.chain_config.chain_id,
            block_hashes=block_hashes,
            parent_header=parent_header,
        )

        pre_state = WitnessState(
            _node_db=build_node_db(witness.state),
            _state_root=parent_header.state_root,
            _code_db=build_code_db(witness.codes),
        )

        execute_new_payload_request(
            stateless_input.new_payload_request,
            pre_state,
            chain_context,
            transaction_public_keys=stateless_input.public_keys,
        )
        successful_validation = True
    except Exception:
        successful_validation = False

    return StatelessValidationResult(
        new_payload_request_root=new_payload_request_root,
        successful_validation=successful_validation,
        chain_config=stateless_input.chain_config,
    )
```

On the host side, `build_stateless_input` receives parts gathered during block re-execution/construction, and constructs the `StatelessInput` that will be SSZ-serialized for the guest program input. 

```python
def build_stateless_input(
    block: Block,
    *,
    execution_witness: ExecutionWitness,
    execution_requests: ExecutionRequests,
    block_access_list: BlockAccessList,
    chain_id: U64,
) -> StatelessInput:
    ...

    new_payload = NewPayloadRequest(
        execution_payload=payload,
        versioned_hashes=tuple(versioned_hashes),
        parent_beacon_block_root=header.parent_beacon_block_root,
        execution_requests=execution_requests,
    )

    return StatelessInput(
        new_payload_request=new_payload,
        witness=execution_witness,
        chain_config=build_chain_config(chain_id),
        public_keys=tuple(public_keys),
    )


def build_chain_config(chain_id: U64) -> ChainConfig:
    return ChainConfig(
        chain_id=chain_id,
        active_fork=ForkConfig(
            fork=ProtocolFork.Amsterdam,
            activation=ForkActivation(
                block_number=None,
                timestamp=U64(0),
            ),
            blob_schedule=BlobSchedule(
                target=BLOB_SCHEDULE_TARGET,
                max=BLOB_SCHEDULE_MAX,
                base_fee_update_fraction=U64(BLOB_BASE_FEE_UPDATE_FRACTION),
            ),
        ),
    )
```

The execution witness is built from the block-level read/write tracker and pre-state trie data. This can be done with the same execution client component used for constructing BALs.

```python
def build_execution_witness(
    block_state: BlockState,
    expected_post_state_root: Root,
    pre_state_accounts_data: Trie[Address, Optional[Account]],
    pre_state_storages_data: Dict[Address, Trie[Bytes32, U256]],
    blockchain_headers: Optional[List[Bytes]] = None,
) -> ExecutionWitness:
    ancestor_headers = get_witness_ancestors(
        blockchain_headers if blockchain_headers is not None else [],
        block_state.oldest_ancestor_offset,
    )
    codes = get_witness_codes(block_state.code_reads, block_state.pre_state)

    incr_storage_mpts = _build_pre_state_storage_mpts(pre_state_storages_data)
    incr_account_mpt = _build_pre_state_account_mpt(
        pre_state_accounts_data, incr_storage_mpts
    )

    all_storage_accesses = _collect_storage_accesses(block_state)
    _capture_pre_state_storage_nodes(incr_storage_mpts, all_storage_accesses)
    _apply_storage_writes(incr_storage_mpts, block_state.storage_writes)

    all_dirty_accounts = _get_all_dirty_accounts(block_state)
    _capture_pre_state_account_nodes(
        incr_account_mpt,
        block_state.account_reads,
        all_dirty_accounts,
    )
    _apply_account_writes(
        incr_account_mpt,
        incr_storage_mpts,
        block_state,
        all_dirty_accounts,
    )

    assert mpt_root(incr_account_mpt) == expected_post_state_root

    accessed_nodes = _collect_accessed_nodes(
        incr_account_mpt, incr_storage_mpts
    )

    return ExecutionWitness(
        state=tuple(sorted(accessed_nodes.values())),
        codes=tuple(codes),
        headers=tuple(ancestor_headers),
    )
```

## Rationale

### Path to low-resource validation

This step moves the network measurably closer to low-resource validation. Stateless, sublinear verification breaks the coupling between a node's hardware requirements and the gas limit or state size, lowering the floor for who can meaningfully run a node and improving decentralisation.

### Build operational experience

The central design choice is to deploy execution proofs as an opt-in feature. Stateless validation, the witness format, and the proof system itself are maturing, but we need operational experience before making execution proofs load-bearing. Treating execution proofs as a non-critical artefact lets the stack mature on a live network — proof sizes, generation latency, verifier throughput, gossip behaviour, prover diversity — without placing any of that on the path of fork choice or attestation. A bug, outage, or poor parameter choice is contained to the nodes that opted in; it cannot fork the chain or affect validators that did not subscribe.

## Backwards Compatibility

This EIP is fully opt-in and does not require a hardfork. No consensus rule changes: validators that do not enable either mode see no change to their behaviour, their bandwidth, or their attestation duties. Nodes that opt in additionally subscribe to the proof gossip topic and may run a prover; this affects only their local resource profile.

## Test Cases

- [Execution Layer](https://github.com/ethereum/execution-specs/tree/ec23140720d6a9257a907c470ba1874623bd7b50/tests/amsterdam/eip8025_optional_proofs)
- [Consensus Layer](https://github.com/ethereum/consensus-specs/tree/5aa6eec83af11bb67e7ff69aa647b5736f530342/specs/_features/eip8025)

## Reference Implementation

- Lighthouse: `https://github.com/eth-act/lighthouse/tree/1480f94c094382ab092362b0b160dc8206acf18f`
- [Execution Layer](https://github.com/ethereum/execution-specs/tree/ec23140720d6a9257a907c470ba1874623bd7b50/src/ethereum/forks/amsterdam)

## Security Considerations

**Gossip surface.** Proofs are carried over a new gossipsub topic with payload size bounded by `MAX_PROOF_SIZE` (defined in `consensus-specs`). Validation, anti-DoS rate limits, and peer scoring for invalid proofs follow the same patterns as other CL gossip topics; a misbehaving peer is bounded and can be downscored.

**Soundness and blast radius.** A forged proof accepted by a verifying node would cause it to accept an invalid execution payload. Because acceptance produces no on-chain artefact and the consensus state-transition function is unchanged, a forged proof cannot fork the chain on its own. The practical blast radius scales with the number of nodes that have opted in *and* disabled local EL re-execution; nodes that continue to re-execute treat the proof as an additional check, so a bad proof has no effect on them. Initial deployment strongly recommends that opted-in nodes continue to run an execution node alongside, keeping the affected validator set small. The proving system's soundness assumptions are explicit and documented at the proof-system level.

**Liveness and critical-path latency.** Proof generation and verification are off the attestation hot path. Implementations running an execution engine alongside MUST NOT delay block validation or attestation production while waiting for a proof; if a proof is missing or late, the node attests using the fork choice determined by the execution engine's re-execution of payloads.

## Copyright

Copyright and related rights waived via [CC0](../LICENSE.md).
